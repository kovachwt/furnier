#!/bin/sh
# Prevent pushing code that fails TypeScript type checking
echo "Running typecheck before push..."
npm run typecheck
if [ $? -ne 0 ]; then
  echo "❌ Typecheck failed. Fix errors before pushing."
  exit 1
fi
echo "✅ Typecheck passed."